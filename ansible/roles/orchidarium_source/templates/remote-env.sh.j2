#! /usr/bin/env bash
# Generated by Ansible. Source this before running Docker Compose on the Raspberry Pi.

export ORCHIDARIUM_UID='{{ ansible_facts["user_uid"] }}'
export ORCHIDARIUM_GID='{{ ansible_facts["user_gid"] }}'
export DEBUG='{{ orchidarium_debug }}'
export ORCHIDARIUM_DOCKER_SUBNET='{{ orchidarium_docker_subnet }}'
export QT_QPA_PLATFORM='{{ orchidarium_qt_qpa_platform }}'
export QT_QUICK_BACKEND='{{ orchidarium_qt_quick_backend }}'
export WAYLAND_DISPLAY='{{ orchidarium_wayland_display }}'
export WAYLAND_RUNTIME_DIR='{{ orchidarium_wayland_runtime_dir }}'

source scripts/.env.sh
