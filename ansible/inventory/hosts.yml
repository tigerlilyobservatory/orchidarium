---
all:
  children:
    orchidarium:
      hosts:
        orchidarium-rpi:
          # The Raspberry Pi is expected at 172.16.0.35/24; SSH uses the host address.
          ansible_host: 172.16.0.35
          ansible_user: tigerlily
          ansible_password: raspberry
          ansible_become_password: raspberry
          ansible_python_interpreter: /usr/bin/python3

